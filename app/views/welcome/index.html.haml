%h1
  Welcome!
- if user_signed_in?
  %strong
    = current_user.username
  you can order lunch on the
  = link_to "dashboard", dashboard_path
- else
  Please
  = link_to "register", new_user_registration_path
  or
  = link_to "sign_in", new_user_session_path